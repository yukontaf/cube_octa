{% set model = dbt_model('int_bloomreach_events_enhanced') %}
 
cubes:
  - {{ model.as_cube() }}
 
    dimensions:
      {{ model.as_dimensions() }}
 
    measures:
      - name: count
        type: count
      - name: distinct_users
        type: count_distinct
        sql: user_id
    
