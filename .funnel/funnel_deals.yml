{% set model = dbt_model('funnel_deals') %}
 
cubes:
  - {{ model.as_cube() }}
 
    dimensions:
      {{ model.as_dimensions() }}
 
    # Model-specific measures
    measures:
      - name: count
        type: count
  
    # Model-specific joins
    # joins:
    #   - name: ab_users_reactivation
    #     sql: "{CUBE.user_id} = {ab_users_reactivation.user_id}"
    #     relationship: many_to_one
 